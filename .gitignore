node_modules/
dist/
dev-dist/
.wrangler/
.dev.vars
.dev.vars.*
# ...but the committed TEMPLATE is the documentation of which secrets a dev needs. It holds
# placeholders only; the real values live in the (ignored) .dev.vars beside it.
!.dev.vars.example
.env
.env.*
*.local
.DS_Store
coverage/

# generated by `wrangler types` (run via `npm run typecheck`)
apps/api/worker-configuration.d.ts

# agent worktrees
.claude/
